import './styles.scss';

function TextInput2({name,type,placeholder}) {
    return (
        < input name={name} type={type} placeholder={placeholder} className="text-input2" />
            
    );
}

export default TextInput2;