import './styles.scss';

function TextInput4({name,type,placeholder}) {
    return (
        < input name={name} type={type} placeholder={placeholder} className="text-input4" />
            
    );
}

export default TextInput4;